let num = 10    /* 0 = False / 1 = True */
         /* verdadeiro / falso */
res = (!(num % 2) ? "Par" : "Impar")      /* '?' é o operador ternario '!' nega 'num % 2' que dara 0 e iria retorna 'False' */

console.log(res)