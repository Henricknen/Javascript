const repositorios = ['HTML', 'SQL', 'JAVASCRIT', 'PHP', 'REACT']
let r = repositorios.map((el, i) => {      /* Variável 'let r' que recebe o retorno do método 'map' */
    return el   /* retornando o elemento */
})

console.log(r)      /* retorna o que será retornado por 'repositorio' */